# Urban Yards Dashboard Future Features

Recommended next passes:

- Add assigned-user fields to jobs, routes, properties, and tasks, then tighten worker RLS from read access to assigned-record access.
- Build full CSV preview/import workflow on top of `import_batches` and `import_errors`.
- Add archive/restore UI for all business modules.
- Add a dedicated Activity Log page with filters for actor, action, module, date range, and entity type.
- Add Mobile Field Mode for quick lead/property/contact/note/photo capture.
- Add user preference controls in Tools & Admin.
- Add scheduled Supabase backup reminders and storage backup documentation.
- Extend AI actions to summarize leads/properties, draft follow-ups, identify stale leads, and prepare walkthrough notes.
- Add optional notification integrations for voicemail, overdue payments, overdue jobs, and website quote requests.
