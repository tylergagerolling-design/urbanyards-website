{
  "name": "urban-yards-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check script.js && node --check assistant.js && node --check dashboard.js && node --check pay.js && node --check scripts/build-dashboard-config.js && node --check netlify/functions/find-square-invoices.js && node --check api/assistant.js && node --check api/quote.js && node --check api/retention-cleanup.js",
    "dev": "netlify dev"
  },
  "engines": { "node": ">=20" }
}
