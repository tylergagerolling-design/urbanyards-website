{
  "name": "urban-yards-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check script.js && node --check assistant.js && node --check dashboard.js && node --check api/assistant.js && node --check api/quote.js && node --check api/retention-cleanup.js",
    "dev": "netlify dev"
  },
  "engines": { "node": ">=20" }
}
