name: Production health check

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - name: Check Urban Yards API health
        run: curl --fail --show-error --silent --retry 2 --max-time 20 https://urbanyards.us/api/health
